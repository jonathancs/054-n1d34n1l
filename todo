studied
    ✅ page.evaluate + if


find ways to verify the presence of an element.
    try
    catch
    $()
    waitForSelector

    JS
        ✅ difference between null, undefined and ______

    Jquery
        //If the length attribute of the element is not 0, then it exists.
            if($('#test').length > 0){
                alert('Element exists!');
            } else{
                alert('Element does not exist!');
            }

    NODE
        //Using document.body.contains.
            if(document.body.contains(document.getElementById('test'))){
                alert('Element exists!');
            } else{
                alert('Element does not exist!');
            }

    puppeteer


